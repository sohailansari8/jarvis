# JARVIS AI Desktop Assistant — Requirements
# Install: pip install -r requirements.txt

# ── Core UI & system ────────────────────────────────────────────────────────
PyQt5>=5.15.0
SpeechRecognition>=3.10.0
wikipedia>=1.4.0
pyautogui>=0.9.54
pyjokes>=0.6.0
requests>=2.28.0
psutil>=5.9.0
pygetwindow>=0.0.9
pyperclip>=1.8.2
Pillow>=9.0.0
pytesseract>=0.3.10
PyAudio>=0.2.14
edge-tts>=7.0.0
pygame>=2.0.0

# ── AI providers — install at least ONE ─────────────────────────────────────

# Option A: Google Gemini (get free key → aistudio.google.com/app/apikey)
google-genai>=2.7.0

# Option B: OpenAI GPT-4o / GPT-4o-mini (get key → platform.openai.com)
# Also used for Groq (same SDK, different base URL)
# pip install openai
openai>=1.0.0

# Option C: Groq — free ultra-fast Llama inference (get key → console.groq.com)
# Uses the openai SDK above, no extra package needed.

# Option D: Ollama — fully local, no key, no internet after model download
# Download from: https://ollama.com/download
# Then run:  ollama pull llama3
# No Python package needed.

# ── Notes ────────────────────────────────────────────────────────────────────
# pytesseract also requires the Tesseract binary.
# Download from: https://github.com/UB-Mannheim/tesseract/wiki
# Default path expected: C:\Program Files\Tesseract-OCR\tesseract.exe
# OCR features are disabled gracefully if Tesseract is not found.
